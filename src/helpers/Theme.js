import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      light: '#ffffff',
      main: '#000000',
      dark: '#828282',
      contrastText: '#fff',
    },
    secondary: {
      light: '#000000',
      main: '#ffffff',
      dark: '#FF0000',
      contrastText: '#000',
    },
  },

});